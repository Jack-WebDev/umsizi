---
"umsizi": minor
---

Add deep object utilities (deepClone, deepEqual, deepMerge, flattenObject, unflattenObject, diffObject); move the object-utilities API reference into per-utility docs/ pages; remove schema() and isRecord() from the public API; sync the JSR (mod.ts) export surface with the npm entry point.
